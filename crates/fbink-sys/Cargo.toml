[package]
name = "fbink-sys"
version.workspace = true
edition.workspace = true
license = "GPL-3.0-or-later"
links = "fbink"
build = "build.rs"

[dependencies]

[build-dependencies]
cc.workspace = true
